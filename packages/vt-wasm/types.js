/**
 * TypeScript types for vt-wasm terminal snapshot data model.
 * Maps to avt's Cell/Pen types with structured span representation.
 *
 * This is the compiled JS output of types.ts.
 * All types are interfaces only — no runtime exports needed.
 * This file exists to satisfy the import map entry:
 *   "#vt-wasm/types": "./packages/vt-wasm/types.js"
 */

// No runtime exports — all type definitions are TypeScript-only.
// Import from this module using: import type { ... } from '#vt-wasm/types'
